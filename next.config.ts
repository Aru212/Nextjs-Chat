import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  output: 'standalone', // Creates a minimal production-ready folder
};
export default nextConfig;
